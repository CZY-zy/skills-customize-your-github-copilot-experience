# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objective

Students will learn to build RESTful APIs using the FastAPI framework in Python. This assignment covers creating endpoints, handling HTTP requests, and returning JSON responses.

## 📝 Tasks

### 🛠️	Basic API Endpoints

#### Description
Create a FastAPI application with basic GET and POST endpoints for managing a simple resource (e.g., a list of items or tasks).

#### Requirements
Completed program should:

- Have a GET endpoint that returns a list of items
- Have a POST endpoint that adds a new item to the list
- Use proper status codes and response models
- Include basic validation for input data


### 🛠️	Advanced API Features

#### Description
Extend the API with path parameters, query parameters, and error handling.

#### Requirements
Completed program should:

- Include a GET endpoint with path parameters to retrieve a specific item
- Add query parameters for filtering or pagination
- Implement proper error handling for not found cases
- Use FastAPI's automatic documentation features